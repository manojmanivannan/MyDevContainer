services:
  devcontainer:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ../..:/workspaces:cached
      - ~/.kube:/home/vscode/.kube:cached

    command: sleep infinity
    # depends_on:
    #   - postgres
    # environment:
    #   SOME_ENV_VAR: "value"
